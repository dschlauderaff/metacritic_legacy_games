require "spec_helper"

RSpec.describe MetacriticLegacyGames do
  it "has a version number" do
    expect(MetacriticLegacyGames::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
